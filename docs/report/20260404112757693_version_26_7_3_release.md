# 26.7.3 版本更新记录

## 任务
将项目与 Paper 插件版本更新为 `26.7.3`，并补充对应更新日志。

## 改动
- 将根项目版本从 `26.7.2` 更新为 `26.7.3`。
- 将 `meoweco-paper` 的 `plugin.yml` 版本同步更新为 `26.7.3`。
- 在 `CHANGELOG.md` 新增 `v26.7.3 - 2026-04-04` 发布日志，说明本次 Paper 26.1.1 兼容与构建策略变更。

## 验证
执行命令：`./gradlew.bat :meoweco-paper:shadowJar`

结果：成功。

## 产物
- `meoweco-paper/build/libs/meoweco-paper-26.7.3.jar`
